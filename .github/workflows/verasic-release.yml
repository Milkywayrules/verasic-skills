name: verasic-release

on:
  push:
    tags:
      - 'v*'

jobs:
  full-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: full automated gate
        run: bash scripts/test-all.sh
